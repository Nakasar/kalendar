/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "lutpdwpnj0n6wfie.public.blob.vercel-storage.com",
        port: "",
      },
    ],
  },
};

export default nextConfig;
